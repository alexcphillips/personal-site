// const fs = require("fs");
// const path = require("path");

// exports.find = (req, res) => {
//   fs.readdir(path.join(__dirname, "../../public/homeImages"), (err, files) => {
//     if (err) throw err;
//     return res.status(200).send(files);
//   });
// };
